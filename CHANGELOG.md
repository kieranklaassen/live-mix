# @kieranklaassen/live-mix

## 0.1.0

### Minor Changes

- [#4](https://github.com/kieranklaassen/live-mix/pull/4) [`139068a`](https://github.com/kieranklaassen/live-mix/commit/139068aa35a15fbf65139129d730ee92f1717d7a) Thanks [@kieranklaassen](https://github.com/kieranklaassen)! - Add the `fdn-reverb` WASM device: kkfonie's Tides 8-line Feedback Delay Network (coprime delays, Hadamard mixing, input diffusion, modulated allpass-interpolated lines, DC blocker, damping) extracted JUCE-free into `cpp/devices/fdn-reverb/fdn_reverb.h`. Params `mix`, `decay`, `damping`, `predelayMs`, `size`, `breathRate`, `breathDepth` are exported as `FDN_REVERB_PARAMS` from `@kieranklaassen/live-mix/dsp`, with `fdnReverbBreathLaw` for UI parity with the Tides breathing law.

- 0.1.0 — Phase 0 complete: engine core (transport, scheduler, clip tracks with both fade families, live-input tracks, sends/returns, ducker, output router with iOS element mode, sample store), the WASM device host with five stock devices (Dattorro, FDN reverb, StereoWidener, zita-rev1, limiter-1176) and the node-device registry, channel strips and groups, automation lanes and modulators, sample eviction and element tracks, the worklet sidechain ducker, and the recording test harness. Adopted by ambient-live and, behind a flag, Breathwork Live.

- [#5](https://github.com/kieranklaassen/live-mix/pull/5) [`c3f958e`](https://github.com/kieranklaassen/live-mix/commit/c3f958e6aaf7619dc73df606e8fd10fd036526a3) Thanks [@kieranklaassen](https://github.com/kieranklaassen)! - Add the `stereo-widener` WASM device: kkfonie's S1-style StereoWidener (source copied unchanged from Felt/Bloom/Sympathetic/Thesis) behind the device ABI, with a single `width` parameter (0 = mono, 0.5 = normal, 1 = allpass decorrelation + micro Haas; bass below 200 Hz kept narrow). Exports `STEREO_WIDENER_PARAMS` and `StereoWidenerParamName` from `@kieranklaassen/live-mix/dsp`; ships `wasm/stereo-widener.wasm`.

- [#13](https://github.com/kieranklaassen/live-mix/pull/13) [`b3d74c4`](https://github.com/kieranklaassen/live-mix/commit/b3d74c45c27e7a6f7d7cbe1a26bb58c8d71026ef) Thanks [@kieranklaassen](https://github.com/kieranklaassen)! - Node devices, device registry and presets (U23).

  - `NodeDevice` hosts stock Web Audio graphs behind the `Device` contract: GainNode input/output, params clamped and ramped over 5 ms (`NODE_DEVICE_RAMP_SECONDS`, the WASM bypass length) on the underlying AudioParams, click-free dry/wet bypass that keeps the chain running. `defineNodeDevice` builds new ones from a param table and a `build(context)` graph.
  - Six stock node devices: `filter` (one biquad, `type` picks the response), `eq3` (low shelf / peaking / high shelf), `parametric-eq` (low cut, four peaking bands, high cut), `compressor` (DynamicsCompressor + make-up, `reductionDb` meter, 6 ms lookahead reported as latency), `delay` (damped feedback loop, linear dry/wet mix) and `utility` (gain, pan, width down to mono, polarity). Each ships `*_PARAMS`, `*_DEVICE`, `*_DESCRIPTOR`, `create*()` and factory presets.
  - `DeviceRegistry` (`register`, `unregister`, `list({ kind, category })`, `describe`, `create(id, ctx, { params, preset, ...factoryOptions })`, `presets`, `capturePreset`, `onChange`) with descriptor validation; the default `devices` registry is pre-seeded with the node devices. `./dsp` adds `wasmDeviceDescriptor`, one `*_DESCRIPTOR` per stock WASM device (dattorro, fdn-reverb, stereo-widener, zita-rev1, limiter-1176), `STOCK_WASM_DEVICES` and `registerStockWasmDevices()`.
  - Presets are serialisable partial param snapshots (`Preset`, `capturePreset`, `applyPreset`, `serializePreset`/`parsePreset`, `listPresets`, `defaultPreset`, `presetParams`); unknown params are dropped and values clamped on load so older presets keep working.
  - `dbToGain` / `gainToDb`.

### Patch Changes

- [#20](https://github.com/kieranklaassen/live-mix/pull/20) [`b5ed908`](https://github.com/kieranklaassen/live-mix/commit/b5ed908b6f4da245c674ed83ed66c912bcd3c930) Thanks [@kieranklaassen](https://github.com/kieranklaassen)! - Engine wiring for automation and memory: `engine.automation` (control-rate loop driving `LaneWriter`s inside the lookahead while playing, resetting them on stop/pause/end) and `engine.modulation` (a `ModMatrix` updated on every control tick, lazily started); a `SampleRetainer` per audio track (`retainSamples`, default on); `engine.addElementTrack`; `activateOutput()` now returns a promise that resolves once element tracks are unlocked; `Clock` gains `setTimeoutFn`/`clearTimeoutFn`.

- [#9](https://github.com/kieranklaassen/live-mix/pull/9) [`af45e84`](https://github.com/kieranklaassen/live-mix/commit/af45e84ae740044b571b1f3650445e59f5df2bf7) Thanks [@kieranklaassen](https://github.com/kieranklaassen)! - Add the Faust toolchain and two stock Faust devices on the WASM device ABI (U22).

  - `scripts/build-faust.sh` builds a pinned Faust 2.88.0 (C++ backend only) from the release tarball and compiles `cpp/faust/*.dsp` to committed C++ (`cpp/faust/generated/`) and generated `ParamSpec` tables (`src/dsp/devices/faust/`); CI job `faust` regenerates and diffs them like the `wasm` job does for the artefacts.
  - `zita-rev1.wasm` (`re.zita_rev1_stereo`: pre-delay, crossover, low/mid decay, damping, mix) and `limiter-1176.wasm` (`co.limiter_1176_R4_stereo`: input gain, output gain) ship next to `dattorro.wasm`; `createZitaReverb`, `createLimiter1176`, their `*_DEVICE` definitions and `*_PARAMS` tables are exported from `@kieranklaassen/live-mix/dsp`.
  - Native and WASM harnesses cover the parameter contract, impulse/step behaviour, param ranges and stability. Integration notes and licence position in `docs/faust-devices.md`.

- [#6](https://github.com/kieranklaassen/live-mix/pull/6) [`e5c0c18`](https://github.com/kieranklaassen/live-mix/commit/e5c0c18925400fbd216922833755a19dbfe0724d) Thanks [@kieranklaassen](https://github.com/kieranklaassen)! - Distribution moves to GitHub Packages (private): `publishConfig.registry` = `npm.pkg.github.com`, release workflow publishes with `GITHUB_TOKEN` and pushes a `next` snapshot when changesets are pending; the `github:` fallback and CI job authenticate with a token. `scripts/ci-local.sh` runs the whole CI matrix locally.

- [#12](https://github.com/kieranklaassen/live-mix/pull/12) [`746b693`](https://github.com/kieranklaassen/live-mix/commit/746b6933db5b9fc3369b3549238699d852209046) Thanks [@kieranklaassen](https://github.com/kieranklaassen)! - `InstrumentTrack` + `engine.addInstrumentTrack`, the `NoteDevice` contract, `note-on`/`note-off` device messages (processor calls optional `device_note_on/off` exports), `WasmDevice.noteOn/noteOff/postMessage`, and a definition-level `processor: { name, url }` so an app can host an app-local worklet on the same ABI.

- [#15](https://github.com/kieranklaassen/live-mix/pull/15) [`1d74b33`](https://github.com/kieranklaassen/live-mix/commit/1d74b33d28ebadfc60e304e29a78ce17133db478) Thanks [@kieranklaassen](https://github.com/kieranklaassen)! - `AudioTrack.fadeOutVoice` (unconditional fade for adapters that keep their own timeline); `ConvolverReverb` joins the device contract (clamped `wet`, `params` option, presets, `CONVOLVER_REVERB_DESCRIPTOR` in `NODE_DEVICES`); `EngineOptions.devices` and `engine.devices` (defaults to the shared registry).

- [#18](https://github.com/kieranklaassen/live-mix/pull/18) [`e5921bc`](https://github.com/kieranklaassen/live-mix/commit/e5921bc497e3f655e2bdf7e725d90dd33fb68a50) Thanks [@kieranklaassen](https://github.com/kieranklaassen)! - `ChannelStrip` on every track (`AudioTrack`, `LiveInputTrack`, `InstrumentTrack`, `ReturnTrack`): input gain → inserts → `StereoPanner` pan → fader → mute/solo gate → destination, post-fader `sends`, every change a `setTargetAtTime` approach (5 ms). Strips create no nodes until first used, so Phase 0 node order is unchanged. `GroupTrack` + `engine.addGroup/group/hasGroup/groups/removeGroup`: a summing strip members route into (nesting, `add/remove`, loop check, dissolve re-routes members). `SoloInPlace` (`engine.solo`): solo-in-place across tracks, groups and returns — soloed strips heard through their normal routing, others ramped to silence, groups pass soloed members, returns solo-safe by default, mute wins.

- [#17](https://github.com/kieranklaassen/live-mix/pull/17) [`5593ba1`](https://github.com/kieranklaassen/live-mix/commit/5593ba16a701263af0244c21a4464c72eda81b05) Thanks [@kieranklaassen](https://github.com/kieranklaassen)! - Audio-thread sidechain ducker (U17): `WorkletDucker` runs envelope detection and the gain inside an AudioWorklet (`dist/worklets/ducker.js`, two inputs: signal and key) with the Phase 0 defaults plus attack/hold/release, depth, key scale and gain time constant as device params; no main-thread timer or `setTargetAtTime` traffic. `engine.addDucker(bus, { mode: 'worklet' })` inserts it post-fader (opt-in; the legacy poll stays the default and unchanged), both implement the new `SidechainDucker` interface. dsp entry: `loadDuckerProcessor`, `createWorkletDucker`, `duckerProcessorUrl`, `ensureProcessor`. `DuckerKernel` exposes the DSP for offline rendering and tests.

- [#16](https://github.com/kieranklaassen/live-mix/pull/16) [`99708e0`](https://github.com/kieranklaassen/live-mix/commit/99708e08793702f7e84240de69d24dfb52b9a266) Thanks [@kieranklaassen](https://github.com/kieranklaassen)! - `SampleStore` eviction (U18, R34): an LRU byte budget (`budgetBytes`, default unbounded), `pin`/`unpin`, counted holds via `retain(id)`, `evictOnRelease`, `evict()`, `onEvict`, `metrics`, `bytes`, `peek`/`touch`/`ids`, `bytesOfBuffer`, and `LoadedSample.kind: 'buffer'`/`bytes`. `SampleRetainer`: a Schedulable that follows a track's preload window and holds each upcoming clip's sample until the clip has ended. `ElementSource` (HTMLMediaElement + MediaElementAudioSourceNode, `prime`/`unlock`/`play`) and `ElementTrack`, which plays element sources with `AudioTrack`'s envelope events and Schedulable contract, with documented timing differences; `ClipSource = LoadedSample | ElementSource`. Testing: `MockMediaElement`, and `createMediaElementSource` rejects a second node per element as browsers do.

- [#14](https://github.com/kieranklaassen/live-mix/pull/14) [`2f5a211`](https://github.com/kieranklaassen/live-mix/commit/2f5a211e251be1b32c647c006a705bf6d32c62b9) Thanks [@kieranklaassen](https://github.com/kieranklaassen)! - Add automation lanes and modulators (U19): `ParamLane` breakpoints with step, linear,
  exponential and smooth curves evaluated offline (`valueAt`, `render`); `LaneWriter`, which
  writes a lane ahead into an AudioParam inside the scheduler window, follows loops, and
  yields to a live override with cancel-and-hold (Firefox fallback included); the modulation
  sources `Lfo` (free-running, Tides' breathing law `1 − depth·(1 − breathMod)`),
  `ExternalPhase` (breath phase), `EnvelopeFollower`, `Random` (seeded sample-and-hold) and
  `Macro`; and `ModMatrix`, which routes sources onto any AudioParam or device parameter with
  depth and polarity as click-free ramps.

- [#1](https://github.com/kieranklaassen/live-mix/pull/1) [`a86aeaf`](https://github.com/kieranklaassen/live-mix/commit/a86aeaf9e687010fc5e226b454ab08f356f7ee72) Thanks [@kieranklaassen](https://github.com/kieranklaassen)! - `./testing`: complete the recorded-AudioParam harness — `createMockContext()`, `advance()`, `configureMocks({ spy, advanceTimers })` for runner integration, and mocks for compressor, delay, oscillator, media-element source and worklet message ports.

- [#2](https://github.com/kieranklaassen/live-mix/pull/2) [`ba23184`](https://github.com/kieranklaassen/live-mix/commit/ba231842681a6deab1be5d9d8b4c17d814ebccfe) Thanks [@kieranklaassen](https://github.com/kieranklaassen)! - Add the clip primitives (U3): the seconds-first `Clip` record, `fadeGain` and the
  equal-power `equalPowerFadeIn`/`equalPowerFadeOut` curves, `computePeaks`/`slicePeaks`
  waveform peaks, and the pure `clipsInWindow` lookahead window — ported from
  ambient-live and Breathwork Live with their tests.

- [#7](https://github.com/kieranklaassen/live-mix/pull/7) [`0d20395`](https://github.com/kieranklaassen/live-mix/commit/0d20395f48d6e0c2d0f63187a6335b89b1ae24a5) Thanks [@kieranklaassen](https://github.com/kieranklaassen)! - Add the transport and scheduler (U4): `Transport` (play, pause, stop with fade, seek,
  loop with numbered passes, position derived from an anchor on the audio clock),
  `Scheduler` (one lookahead timer over `Schedulable`s with per-schedulable
  `lookaheadSec`, idempotent `clipId:iteration:startSec` keys, and catch-up from the
  anchor after a throttled timer), plus `positionFromAnchor`, `startsInWindow`,
  `scheduleKey`, `wrapPosition` and `isLooping` — ported from ambient-live's
  `use-clip-transport` with parity tests against it and against Breathwork Live's
  `MusicEngine` handoff rule.

- [#10](https://github.com/kieranklaassen/live-mix/pull/10) [`e0f8a9a`](https://github.com/kieranklaassen/live-mix/commit/e0f8a9a2e37170500c9894b2cd2f279a32ae9f92) Thanks [@kieranklaassen](https://github.com/kieranklaassen)! - `SampleStore` (fetch/decode cached by id, optional peaks) and `AudioTrack` clip playback with both envelope families verbatim — ambient-live's linear late-join voices and Breathwork Live's equal-power `setValueCurveAtTime` voices with the ±12 dB trim — plus `ClipList`, playback/preload Schedulables for the engine `Scheduler`, `fadeOut`/`stop`/`stopPending`/`stopAll`, `engine.addAudioTrack`, `engine.samples`, `engine.stop({ fadeSec })`.

- [#8](https://github.com/kieranklaassen/live-mix/pull/8) [`c08b305`](https://github.com/kieranklaassen/live-mix/commit/c08b305b34b212bdbd7fee3a2d77b24963974aaa) Thanks [@kieranklaassen](https://github.com/kieranklaassen)! - Core spine: `createEngine`/`Engine` (adopts a context, owns `OutputRouter`, `MasterBus`, named `Bus`es, the `Transport` and `Scheduler`, and an injectable clock/timers), `OutputRouter` with iOS element mode + `MediaSession` (lifted from Breathwork Live), `Bus`/`MasterBus` with post-fader inserts, `Meter`. `Scheduler` accepts injectable `setIntervalFn`/`clearIntervalFn`.

- [#11](https://github.com/kieranklaassen/live-mix/pull/11) [`2fc43ef`](https://github.com/kieranklaassen/live-mix/commit/2fc43eff7bbc7cf7293ac5a6fd3989406f0e8864) Thanks [@kieranklaassen](https://github.com/kieranklaassen)! - `LiveInputTrack` (MediaStream or node, dry gain per attach, sends re-wired on re-attach, `onAttach` hook), `ReturnTrack` + `SendList`, `ConvolverReverb` with Breathwork Live's generated hall IR, main-thread `Ducker` with the identical envelope follower and `setTargetAtTime` writes; `engine.addLiveInputTrack/addReturnTrack/addDucker`, `engine.stop()` stops duckers.

- [#3](https://github.com/kieranklaassen/live-mix/pull/3) [`badbf28`](https://github.com/kieranklaassen/live-mix/commit/badbf285e9ac2f5fec286c8c2fa487b76e051844) Thanks [@kieranklaassen](https://github.com/kieranklaassen)! - `./dsp`: main-thread `WasmDevice` host (`WasmDevice.create`, `defineWasmDevice`) with module compiled once per page and the processor loaded once per context; lazy asset resolution with `{ processorUrl, wasm }` overrides; in-thread click-free `bypass`; `Device` contract and `ParamSpec` in core; `createDattorroReverb(ctx, options)`.
