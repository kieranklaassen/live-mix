---
'@kieranklaassen/live-mix': patch
---

One `ensureProcessor` (add a worklet module once per context) in core, used by the WASM device host, the worklet ducker and the LUFS meter; exported from `.` and `./dsp`.
