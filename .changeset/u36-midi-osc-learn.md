---
'@kieranklaassen/live-mix': patch
---

MIDI and OSC learn (U36, R15). `src/core/control` on the `.` entry: `ControlTarget` (strip level/pan/mute/solo/trim by track name, send level, master, device parameter by registered instance id with taper-aware normalisation, macro, transport action) and `ControlSource` (MIDI note, CC, 14-bit CC pair, pitch bend, channel pressure with omni channels; OSC address pattern + argument index); a pure, immutable `MappingTable` (`createMapping`, `mapTarget`, `unmapTarget`, `unmapSource`, `updateMapping`, `resolveControlEvent`) with `set`/`toggle`/`relative` modes, input ranges, output spans, curves, soft takeover (`pickup`) and relative-encoder encodings; learn (`learnFromEvent`, 7-bit → 14-bit upgrade); versioned serialisation with migration hooks and a `StorageLike` adapter (`serializeMappingTable`, `parseMappingTable`, `loadMappingTable`, `saveMappingTable`, `ambientLiveMidiMapMigration` for ambient-live's U27 format-1 tables); `MidiDecoder` (running status, 14-bit pairing, `decodeRelative`) and an OSC 1.0 codec (`decodeOscPacket` incl. nested bundles, `encodeOscMessage`, `matchOscAddress`); `MidiInput` over Web MIDI and `OscInput` over WebSocket, both injectable, neither touching a browser global at import time; and `ControlSurface`, which owns the table, learn state, device/macro registries and attached `LaneWriter`s, dispatches through the engine's ramped setters and overrides automation with cancel-and-hold (R29) until `release`. `normalizeParam` / `denormalizeParam` moved to core (`.` entry; still exported from `./react`). `./react` adds `useControlSurface` and `useLearn`. Docs: `docs/control-surface.md` with the ambient-live migration path.
