---
'@kieranklaassen/live-mix': patch
---

`./testing`: complete the recorded-AudioParam harness — `createMockContext()`, `advance()`, `configureMocks({ spy, advanceTimers })` for runner integration, and mocks for compressor, delay, oscillator, media-element source and worklet message ports.
